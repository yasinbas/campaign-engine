package com.opensource.campaignengine.domain;

public enum ProductSaleType {
    BY_UNIT,    // Adet ile satılan
    BY_WEIGHT,  // Ağırlık ile satılan (kg)
    BY_VOLUME   // Hacim ile satılan (lt)
}